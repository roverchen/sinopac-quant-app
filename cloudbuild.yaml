steps:
  # 1. Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/quant-pro', '.']

  # 2. Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/quant-pro']

  # 3. Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'quant-pro'
      - '--image'
      - 'gcr.io/$PROJECT_ID/quant-pro'
      - '--region'
      - 'asia-east1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '1'

  # 4. Clear GCS historical cache to ensure fresh data on new version
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - 'gcloud storage rm gs://$PROJECT_ID-data/shared_results_*.pkl || exit 0'

images:
  - 'gcr.io/$PROJECT_ID/quant-pro'

options:
  logging: CLOUD_LOGGING_ONLY
